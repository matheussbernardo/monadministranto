version: 0.0
os: linux
files:
  - source: ./
    destination: /home/bot

hooks:
  ApplicationStart:
    - location: scripts/start_server
      timeout: 300
      runas: root
  ApplicationStop:
    - location: scripts/start_server
      timeout: 300
      runas: root